# Python CLI Project

Example target project for AI Team bootstrap.

## What this project is

A small command-line tool used to test install and bootstrap behavior.

## Stack

- Python
- `pyproject.toml`
- pytest-style tests

## Expected AI Team context

An agent should infer that this is a Python CLI project, record local commands in `.ai/context/stack.md`, and avoid running them unless the user permits runtime checks.
