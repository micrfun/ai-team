[project]
name = "python-cli-project"
version = "0.0.0"
description = "Example Python CLI target for AI Team bootstrap."
requires-python = ">=3.11"

[project.scripts]
example-cli = "example_cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
